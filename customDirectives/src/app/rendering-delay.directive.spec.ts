import { RenderingDelayDirective } from './rendering-delay.directive';

describe('RenderingDelayDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderingDelayDirective();
    expect(directive).toBeTruthy();
  });
});
